# RailMatch - Система управления железнодорожной логистикой

Передовая платформа для эффективного управления грузоперевозками, маршрутами и расписанием железнодорожных отправлений.

## 📋 Технологический стек

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Статическая типизация
- **Prisma ORM** - Система управления базой данных с SQLite
- **TailwindCSS** - Утилитарный CSS фреймворк
- **ESLint & Prettier** - Линтинг и форматирование кода

## 🚀 Быстрый старт

### Системные требования

- Node.js 18+
- npm 9+ или yarn 3+

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd railmatch
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env.local`:**
   ```bash
   cp .env.example .env.local
   ```

4. **Заполните переменные окружения:**
   Отредактируйте `.env.local` и добавьте необходимые значения:
   - `DATABASE_URL` - строка подключения к базе данных (по умолчанию `file:./dev.db` для SQLite)
   - `NEXTAUTH_SECRET` - секретный ключ для аутентификации
   - `NEXTAUTH_URL` - URL вашего приложения

5. **Настройте базу данных:**
   ```bash
   # Генерация Prisma Client
   npx prisma generate
   
   # Создание миграций и применение схемы
   npx prisma migrate dev --name init
   
   # Заполнение базы данных начальными данными
   npm run seed
   ```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📦 Доступные скрипты

- `npm run dev` - Запуск dev-сервера с hot reload
- `npm run build` - Создание production-ready сборки
- `npm start` - Запуск production-сервера
- `npm run lint` - Проверка кода согласно ESLint правилам
- `npm run seed` - Заполнение базы данных начальными данными
- `npx prisma generate` - Генерация Prisma Client
- `npx prisma migrate dev` - Создание и применение миграций
- `npx prisma studio` - Открытие GUI для просмотра данных

## 📁 Структура проекта

```
railmatch/
├── app/
│   ├── (public)/           # Public routes
│   │   └── page.tsx        # Landing page
│   ├── layout.tsx          # Root layout с header/footer
│   ├── globals.css         # Глобальные стили
│   └── ...
├── components/
│   └── ui/                 # Переиспользуемые UI компоненты
│       ├── Button.tsx
│       ├── Card.tsx
│       ├── Input.tsx
│       ├── Textarea.tsx
│       └── Select.tsx
├── lib/
│   ├── i18n/              # Интернационализация
│   │   └── ru.ts          # Русский перевод
│   └── prisma.ts          # Prisma Client singleton
├── prisma/
│   ├── schema.prisma      # Схема базы данных
│   ├── seed.ts            # Seed скрипт для БД
│   └── migrations/        # История миграций
├── public/                # Статические файлы
├── scripts/               # Утилиты и скрипты
├── .env.example           # Пример переменных окружения
├── .eslintrc.json         # ESLint конфигурация
├── .prettierrc.json       # Prettier конфигурация
├── next.config.mjs        # Next.js конфигурация
├── postcss.config.mjs     # PostCSS конфигурация
├── tailwind.config.ts     # Tailwind конфигурация
└── tsconfig.json          # TypeScript конфигурация
```

## 🎨 UI Компоненты

Проект включает базовый набор переиспользуемых UI компонентов:

### Button
```tsx
import { Button } from '@/components/ui/Button';

<Button variant="primary">Начать</Button>
<Button variant="outline">Отмена</Button>
```

### Card
```tsx
import { Card } from '@/components/ui/Card';

<Card>
  <p>Содержание карточки</p>
</Card>
```

### Input
```tsx
import { Input } from '@/components/ui/Input';

<Input 
  label="Имя"
  placeholder="Введите имя"
  error="Обязательное поле"
/>
```

### Textarea
```tsx
import { Textarea } from '@/components/ui/Textarea';

<Textarea 
  label="Описание"
  placeholder="Введите описание"
/>
```

### Select
```tsx
import { Select } from '@/components/ui/Select';

<Select 
  label="Выберите опцию"
  options={[
    { value: '1', label: 'Опция 1' },
    { value: '2', label: 'Опция 2' },
  ]}
/>
```

## 🗄️ База данных

Проект использует **Prisma ORM** с **SQLite** для локальной разработки. База данных моделирует сущности логистической платформы для железнодорожных перевозок.

### Схема базы данных

#### Модели:
- **User** - Пользователи системы (администраторы, операторы, грузоискатели)
- **Company** - Компании (операторы и грузоискатели)
- **Offer** - Предложения от операторов (доступные вагоны)
- **Request** - Заявки от грузоискателей (потребность в перевозке)
- **Match** - Совпадения между предложениями и заявками с AI-скорингом
- **Thread** - Переписка по совпадениям/заявкам
- **Message** - Сообщения в диалогах
- **DealStatusHistory** - История изменения статусов сделок
- **AnalyticsSnapshot** - Снимки аналитических данных по компаниям

#### Енумы:
- **UserRole** - Роли пользователей (ADMIN, OPERATOR, SEEKER)
- **DealStatus** - Статусы сделок (PENDING, NEGOTIATING, ACCEPTED, REJECTED, COMPLETED, CANCELLED)
- **WagonType** - Типы вагонов (TANK, HOPPER, FLATCAR, BOXCAR, GONDOLA, REFRIGERATOR, PLATFORM)
- **CargoType** - Типы грузов (COAL, OIL, GRAIN, METAL, CHEMICAL, TIMBER, CONTAINER, BULK, OTHER)
- **MatchStatus** - Статусы совпадений (PENDING, ACCEPTED, REJECTED, EXPIRED)

### Seed данные

При запуске `npm run seed` создаются:
- 2 компании (1 оператор + 1 грузоискатель)
- 4 пользователя (2 оператора, 1 грузоискатель, 1 администратор)
- 3 предложения от операторов
- 3 заявки от грузоискателей
- 3 совпадения между предложениями и заявками
- 2 диалога с сообщениями
- История статусов сделок
- Аналитические снимки

Все данные на русском языке с реалистичными названиями станций и регионов России.

### Работа с базой данных

```tsx
// Использование Prisma Client в коде
import prisma from '@/lib/prisma';

// Получение всех предложений
const offers = await prisma.offer.findMany({
  include: {
    company: true,
    createdBy: true,
  },
});

// Создание нового предложения
const newOffer = await prisma.offer.create({
  data: {
    companyId: 1,
    createdById: 1,
    wagonType: 'GONDOLA',
    cargoType: 'COAL',
    wagonCount: 50,
    // ... другие поля
  },
});
```

## 🌐 Интернационализация

Проект использует русский язык как язык по умолчанию. Все текстовое содержание централизовано в `lib/i18n/ru.ts`:

```tsx
import { copy } from '@/lib/i18n/ru';

<h1>{copy.hero.title}</h1>
```

## 🛠️ Разработка

### Форматирование кода

```bash
npm run lint
```

### Типы для TypeScript

При добавлении новых компонентов убедитесь, что:
- Все пропсы правильно типизированы
- Компоненты экспортируются как named exports
- Используются React.forwardRef для компонентов форм

## 📝 Цвета и дизайн система

Проект использует нейтральную палитру, подходящую для логистики UI:

- **Primary (Синий)** - основные действия и важные элементы
- **Neutral (Серый)** - текст, фоны, бордеры
- **Red** - ошибки и предупреждения

Все цвета определены в `tailwind.config.ts` и готовы к использованию.

## 🚦 Следующие шаги

- [x] Настроить базу данных (Prisma ORM с SQLite)
- [x] Создать схему базы данных для MVP
- [x] Настроить seed скрипт с тестовыми данными
- [ ] Реализовать аутентификацию (NextAuth.js)
- [ ] Добавить API маршруты для CRUD операций
- [ ] Реализовать каталог предложений и заявок
- [ ] Добавить AI матчинг между предложениями и заявками
- [ ] Реализовать систему диалогов
- [ ] Добавить аналитические дашборды

## 📞 Поддержка

Для поддержки свяжитесь с командой разработки или создайте issue в репозитории.

## 📄 Лицензия

Собственность RailMatch © 2024
